# ──────────────────────────────────────────────────────────────────────────────
# Vethub — Caddy reverse proxy configuration
#
# Auto-provisions TLS via Let's Encrypt. No manual cert management needed.
#
# For local dev without DNS, you can run:
#   caddy run --config Caddyfile
# but you'll need to set your domain or use `tls internal` for dev.
# ──────────────────────────────────────────────────────────────────────────────

# ── Production ────────────────────────────────────────────────────────────────
your-domain.com {
    # Reverse proxy to Bun app
    reverse_proxy localhost:3000

    # Security headers
    header {
        # HSTS (6 months)
        Strict-Transport-Security "max-age=15768000; includeSubDomains"
        # Prevent MIME sniffing
        X-Content-Type-Options "nosniff"
        # Prevent clickjacking
        X-Frame-Options "SAMEORIGIN"
        # XSS protection
        X-XSS-Protection "1; mode=block"
        # Referrer policy
        Referrer-Policy "strict-origin-when-cross-origin"
        # Permissions policy
        Permissions-Policy "camera=(), microphone=(), geolocation=()"
    }

    # Gzip compression
    encode gzip

    # Logs
    log {
        output file /var/log/caddy/vethub.log
    }
}

# ── Staging (optional — uncomment and use for testing) ────────────────────────
# staging.your-domain.com {
#     reverse_proxy localhost:3000
#     header { ... }
#     encode gzip
#     tls {
#         # Use staging CA to avoid rate limits during testing
#         ca https://acme-staging-v02.api.letsencrypt.org/directory
#     }
# }
