# Dockerfile inputs
.git
.gitignore
.github
.dockerignore
Dockerfile
docker-compose*.yml
nginx/

# Project meta
README.md
LICENSE
.env
.env.*
*.log
npm-debug.log*
pnpm-debug.log*

# Legacy monorepo (kept in-repo for reference but not needed in the image).
_legacy/

# Backend (FastAPI proxy is only used inside the Emergent pod — not in prod
# Docker since the TanStack Start app serves /api/* directly).
backend/

# Frontend caches / artefacts that should be rebuilt inside the image.
frontend/node_modules
frontend/dist
frontend/.vite
frontend/.output
frontend/.cache
frontend/.tanstack
frontend/coverage
frontend/test-results
frontend/playwright-report
frontend/prisma/dev.db
frontend/prisma/dev.db-journal
frontend/src/routeTree.gen.ts

# Editor / OS junk
.vscode
.idea
.DS_Store
Thumbs.db

# Tests
**/__tests__
**/*.test.ts
**/*.spec.ts
test_reports/
tests/

# Memory / PRD scratch
memory/

# Tooling
scripts/
